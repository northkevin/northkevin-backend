[phases.setup]
nixPkgs = ['nodejs', 'nodejs-20_x']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npm start'

[variables]
NODE_ENV = "production"
PORT = "8080"
# Don't put sensitive data here 